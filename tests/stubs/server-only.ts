/**
 * テスト実行時の `server-only` の差し替え。
 *
 * `server-only` はクライアントコンポーネントからの読み込みをビルド時に止めるための
 * パッケージで、読み込まれると例外を投げる。サービス層にはこの保護を残したいが、
 * そのままだと vitest からも読めなくなるため、テストではこの空モジュールに差し替える。
 * 差し替えは vitest.config.mts の alias で行う。
 */
export {}
