---
description: 設計方針をFable 5.1に査読してもらう(実装前)
model: claude-fable-5-1
argument-hint: [タスク番号と設計方針の要約]
allowed-tools: Read, Glob, Grep
---

あなたは査読役です。以下の設計方針を、実装に入る前に査読してください。

## 前提として読むもの

- `docs/segrr-requirements-v1.md` — 要件定義書(このプロジェクトの正)
- `docs/phase0-task-breakdown.md` — タスク分解
- `docs/db-constraints.sql` — 金額・消費税・テナント整合性のDB制約
- `docs/design-spec.md` — UIの仕様

## 査読対象

$ARGUMENTS

## 出力の形式

指摘を **深刻度 × 確信度** でランク付けし、上位から並べてください。1件ずつ次を書きます。

1. 何が問題か(1〜2文)
2. どう壊れるか(具体的な入力と結果)
3. 提案する修正

## 守ること

- **パッチを当てないでください。** 指摘までが役割で、適用はPMの承認後に実装側が行います
- 要件定義書セクション5の失敗パターンに該当するものは、その番号を明記してください
- 「一般論として望ましい」だけの指摘は出さないでください。このプロジェクトで実際に壊れるものに絞ります
- 指摘が多くなりすぎる場合は、上位7件までに絞ってください
